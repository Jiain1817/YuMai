# 离线下载功能说明

## 1. 功能概述
用户可以在故事详情页将故事下载到本地，之后在“离线模式”页面查看和阅读已下载的故事，无需网络连接。

## 2. 使用方法

### 2.1 下载故事
- 进入任意故事详情页。
- 点击顶部工具栏的 **下载图标**（⬇️）。
- 提示“已下载：故事标题”即表示成功。

### 2.2 查看已下载故事
- 点击底部导航栏的 **离线模式**（或从个人中心进入）。
- 列表显示所有已下载的故事，按下载时间倒序排列。

### 2.3 阅读离线故事
- 在离线模式页面点击故事卡片，进入离线阅读器。
- 阅读器支持三语切换（跟随全局语言设置），内容为下载时保存的正文。

### 2.4 删除离线故事
- 在离线模式页面，点击故事卡片右上角的 **删除图标**（✖️）。
- 确认后该故事将从本地移除。

## 3. 技术实现

### 3.1 存储方案
- 使用 `shared_preferences` 存储离线故事数据。
- 存储键：`yumai_offline_stories`，值为一个 JSON 对象，键为故事 ID，值为故事详情（包含标题、民族、正文等）。

### 3.2 核心服务
- `OfflineStorageService`（位于 `lib/services/offline_storage_service.dart`）提供：
  - `saveStory(Story story)`：保存故事
  - `getAllStories()`：获取所有离线故事
  - `deleteStory(String storyId)`：删除单个故事
  - `isDownloaded(String storyId)`：检查是否已下载

### 3.3 UI 组件
- `DownloadsScreen`：离线模式页面，展示已下载故事列表。
- `OfflineReaderScreen`：离线阅读页面，根据当前语言显示对应正文。

## 4. 注意事项
- 首次下载时请确保网络正常，后续阅读无需网络。
- 如果故事正文很大（超过 1MB），建议改用 `sqflite` 或文件存储（当前故事文本量小，`shared_preferences` 足够）。
- 下载后若更新故事内容（如后端修改了文本），本地不会自动同步，需重新下载。

## 5. 常见问题

**Q: 点击下载按钮没有反应？**
A: 检查控制台是否有报错，确保 `OfflineStorageService` 已正确导入，并且 `Story` 模型字段匹配。

**Q: 离线列表为空，但明明下载了？**
A: 尝试完全重启应用（`flutter run`），并确认 `SharedPreferences` 中没有数据损坏。

**Q: 离线阅读时正文显示空白？**
A: 检查下载时保存的 `content_zh`/`content_yi`/`content_zang` 字段是否为空，或者全局语言设置是否对应。

## 6. 后续优化建议
- 增加下载进度提示。
- 支持批量下载和删除。
- 将离线存储迁移到 `sqflite` 以支持更大数据量。
- 增加离线故事封面图片的本地缓存。

## 7. 相关文件
- `lib/services/offline_storage_service.dart`
- `lib/screens/downloads_screen.dart`
- `lib/screens/offline_reader_screen.dart`
- `lib/screens/story_detail_screen.dart`（下载按钮部分）